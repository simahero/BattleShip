const CONNECT = 'connection'
const DISCONNECT = 'disconnect'
const RESPONSE = 'response'
const ERROR = 'error'
const CREATE_ROOM = 'create_room'
const GAME_CODE = 'game_code'
const JOIN_ROOM = 'join_room'
const DISCONNECT_ROOM = 'disconnect_room'
const START_GAME = 'start_game'
const GAME_OVER = 'game_over'
const IN_GAME = 'in_game'
const RESIZE_GRID = 'resize_grid'
const HIT = 'hit'
const ROOM_SIZE = 6
const CODE_LENGTH = 6
const TIMER = 20
const FPS = 10

module.exports = {
    CONNECT,
    DISCONNECT,
    RESPONSE,
    ERROR,
    CREATE_ROOM,
    GAME_CODE,
    JOIN_ROOM,
    DISCONNECT_ROOM,
    START_GAME,
    GAME_OVER,
    IN_GAME,
    RESIZE_GRID,
    HIT,
    ROOM_SIZE,
    CODE_LENGTH,
    TIMER,
    FPS
}